import { Exo, Roboto_Condensed } from "next/font/google";

export const fontHeading = Exo({
  display: "swap",
  subsets: ["vietnamese"],
  variable: "--font-heading",
});

export const fontBody = Roboto_Condensed({
  display: "swap",
  subsets: ["vietnamese"],
  variable: "--font-body",
});
